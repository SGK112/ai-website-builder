# Handoff — webstew security/feature hardening sweep

## State
All work below is **committed AND pushed to `origin/main` and confirmed live on Render** (latest live deploy = `76b2082`). Nothing is pending in git except an untracked `HANDOFF.md` from a prior session (ignore/leave it).

Full detail of every fix lives in:
- **Git history** `cb87010..76b2082` (16 commits, each message is self-describing — read those, don't re-derive).
- **Memory** (auto-loaded next session): `project-money-tab-hardening.md` and `project-feature-hardening-sweep.md` under the project memory dir. These two files are the authoritative running record — READ THEM FIRST.

## What this work is
Josh asked for a systematic, **tab-by-tab** security + correctness hardening sweep, shipping each tab as its own focused pass. Method used each tab: spawn a read-only audit subagent → verify findings against real code → fix only the safe/clear ones → `tsc --noEmit` → commit → push → poll Render until live. Keep going until told to stop.

## Tabs completed (all live)
money/billing · builder credit-overspend · auth · publish · Video Studio · deploy/export · Bridge · integrations(MCP/Composio) · backend SDK. See the two memory files for the per-tab fix list + what was verified SAFE + per-tab DEFERRED items.

## Open / next (in priority order)
1. **CRITICAL, needs Josh's infra decision — published-site same-origin.** Every published site serves from `www.webstew.net/s/<slug>` (same origin as the app), so a malicious published site's JS can ride a logged-in visitor's session cookie + read shared localStorage. The fix Josh leaned toward = a SEPARATE serving domain (he owns `webstew.app`; needs DNS+TLS, then flip env `PUBLISH_USE_SUBDOMAIN=1`). Code side: `lib/publish.ts` already gates the subdomain URL behind that env var. The CSP-sandbox stopgap was rejected (breaks published-app localStorage). **Do NOT ship CSP sandbox.** Wait for Josh to point DNS, then do the serve-route origin wiring.
2. Lower-severity deferred items (documented in `project-feature-hardening-sweep.md`): checkout server-side product catalog (`api/backend/[appId]/checkout` accepts client prices), video/DALL-E charge-by-resolution, builder reserve-before-stream (concurrency overspend residual), single-use reset tokens + login rate-limit, namespace webstew Composio entity. None urgent.
3. Untouched candidates if continuing the sweep: CMS, forms.

## Critical working-style facts (Josh's preferences — honor these)
- **Money model (confirmed, don't re-litigate):** money IN never OUT. Credits bought with cash, spent on platform, NEVER cashed out. Marketplace = REAL money via Stripe Connect Express (buyer card → seller's own account, platform keeps 3% application fee, platform never pays out). Platform Render hosting is the product default; download-zip + connect-own-Git are alternatives — do NOT require BYO keys for hosting.
- **Don't over-complicate.** Josh explicitly warned against turning fixes into "a tech nightmare that doesn't work." Prefer the simplest backward-compatible fix (e.g. the backend ownerId fix needed zero SDK change because the client already sent the token).
- **Terse, structural, batch-approves.** Present ranked options + recommendation, then execute; don't poll per-step. He corrects fast when wrong (e.g. he reverted my BYO-Render-key requirement).
- **OWL linter:** keep `app/workspace/page.tsx` (~14k lines) thin — new logic → hooks/components (e.g. `MobileAccountMenu.tsx`, `MobileToolCarousel.tsx`).

## Deploy gotcha (important — caused a long "nothing is changing" episode)
Render `autoDeploy:yes` AND the GitHub Action BOTH deploy on every push → two builds per commit that cancel each other; rapid successive pushes cancel in-flight builds so nothing finishes. **Therefore: push ONCE, then WAIT for it to go live before pushing again.** Verify live via the Render API:
- Recover a working key: `ps eww | grep -oE 'rnd_[A-Za-z0-9]+'` then TEST each (shell `RENDER_API_KEY` is stale/401). The one that worked all session: `rnd_NariXdXZFOHfSAsPc9zUu9wRhUZn` (may rotate — re-derive if 401).
- Service id: `srv-d554is8gjchc7388svd0`. Poll: `GET https://api.render.com/v1/services/$SVC/deploys?limit=1` → `[0].deploy.status` == `live` and `.commit.id` matches your HEAD.
- A build takes ~8 min. Josh tests on his phone at `www.webstew.net/workspace`; tell him to hard-refresh (Safari caches the bundle).

## Suggested skills for next session
- `verify` if Josh reports something broke from these changes (run the app, observe).
- `diagnose` for any hard bug/regression.
- `code-review` before a big new batch.
- Continue using the audit-subagent → verify → fix → push → poll loop for any further tabs.
