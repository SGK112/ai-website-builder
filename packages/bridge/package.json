{
  "name": "@webstew/bridge",
  "version": "0.1.0",
  "description": "Local bridge that lets the Webstew workspace chat run against your installed Claude Code (Pro/Max subscription) instead of paying API rates.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "webstew-bridge": "./src/cli.ts"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "dev": "tsx src/cli.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.11.16",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
